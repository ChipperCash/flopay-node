{
  "name": "flopay-node",
  "version": "0.0.1",
  "description": "Unofficial Node SDK for the Flopay API",
  "main": "index.ts",
  "repository": "https://github.com/chippercash/flopay-node",
  "author": "Chipper Engineering <engineering@chipper.cash>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "test": "yarn run lint && node_modules/.bin/jest --all --coverage",
    "build": "yarn run test && node_modules/.bin/tsc",
    "lint": "node_modules/.bin/tslint -p . --fix"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/node-fetch": "^2.1.7",
    "jest": "^24.5.0",
    "standard": "^12.0.1",
    "ts-jest": "^24.0.1",
    "ts-node": "^8.0.3",
    "tslint": "^5.14.0",
    "tslint-config-standard": "^8.0.1",
    "typescript": "^3.3.4000"
  },
  "dependencies": {
    "node-fetch": "^2.3.0"
  }
}
